# SMTP Configuration for Email Services
# Copy this file to .env and fill in your actual values

# SMTP Server Configuration
SMTP_HOST=smtp.gmail.com
# SMTP_HOST=smtp.outlook.com
# SMTP_HOST=smtp.mailgun.org

# SMTP Port (587 for STARTTLS, 465 for SSL/TLS)
SMTP_PORT=587

# SMTP Authentication
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password-or-smtp-password

# SMTP Security (true for SSL/TLS on port 465, false for STARTTLS on port 587)
SMTP_SECURE=false

# Email sender address (usually same as SMTP_USER)
MAIL_FROM=your-email@example.com

# Notes:
# - For Gmail: Use App Passwords instead of your regular password
# - For Outlook: Use your email password or App Password if 2FA is enabled
# - For custom SMTP: Contact your provider for correct settings
# - Never commit this file with real credentials to version control